export interface User {
  id: number;
  name: string;
  email: string;
}

export interface Product {
  name: string;
  capacity: string;
  price: number;
  currency: string;
  product_code: string;
}
